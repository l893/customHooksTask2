# Хук useLocalStorage

Кастомный React-хук для удобной работы с браузерным localStorage. Обеспечивает синхронизацию состояния React с данными в локальном хранилище.

## Пример использования

```typescript
import { useLocalStorage } from './hooks/use-local-storage';

function App() {
  const [theme, { setItem }] = useLocalStorage('theme', 'light');

  return (
    <div>
      <p>Текущая тема: {theme}</p>
      <button onClick={() => setItem('dark')}>Тёмная</button>
      <button onClick={() => setItem('light')}>Светлая</button>
    </div>
  );
}
```

## API

### Параметры

- key: string - ключ в localStorage
- defaultValue?: string - значение по умолчанию (опционально)

### Возвращает

Массив из двух элементов:

1. value: string | null — текущее значение из localStorage или null, если ключ отсутствует

2. actions — объект с методами управления:

- setItem(value: string): void — сохранить новое значение
- removeItem(): void — удалить значение по ключу

### **Установка и использование**

## 🚀 Быстрый старт

1. Скопируйте файлы в проект:

   ```bash
   cp use-local-storage.ts src/hooks/
   cp types.ts src/types/

   ```

2. Импортируйте хук:

```typescript
import { useLocalStorage } from './hooks/use-local-storage';
```

3. Используйте в компонентах:

```typescript
const [value, actions] = useLocalStorage('ключ', 'значение по умолчанию');
```

### Особенности

- Дефолтные значения: При первом рендере, если ключ отсутствует и указан defaultValue, он автоматически сохраняется в localStorage
- Обработка ошибок: Хук корректно обрабатывает случаи, когда localStorage недоступен (приватный режим браузера)
- Только строки: В localStorage хранятся только строки. Для объектов используйте JSON.stringify() при сохранении и JSON.parse() при чтении
